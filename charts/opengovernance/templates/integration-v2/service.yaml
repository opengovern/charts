apiVersion: v1
kind: Service
metadata:
  name: integration-v2-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: integration-v2-service
    monitoring: kaytu
spec:
  selector:
    app: integration-v2-service
  ports:
  - name: http
    port: 8000
    targetPort: 8000